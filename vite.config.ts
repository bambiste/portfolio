// @fluixi/start owns the build plugins (routing + JSX/SSR). Using the published @fluixi/*
// packages, so resolution goes through their default `import` exports (dist) — no source
// `development` condition here (that only exists inside the monorepo).
export default {};
